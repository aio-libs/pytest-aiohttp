[build-system]
requires = [
  "setuptools>=82.0.1",
  "wheel>=0.47.0",
  "setuptools_scm>=6.2"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "pytest_aiohttp/_version.py"
