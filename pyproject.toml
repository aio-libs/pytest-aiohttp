[build-system]
requires = [
  "setuptools>=83.0.0",
  "wheel>=0.47.0",
  "setuptools_scm>=10.2.1"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "pytest_aiohttp/_version.py"
